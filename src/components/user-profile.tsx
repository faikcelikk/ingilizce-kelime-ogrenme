"use client";

/**
 * Kullanıcı profili bileşeni.
 * Firebase auth kaldırıldığından bu bileşen artık aktif kullanılmıyor.
 * Gerekirse yerel kullanıcı tercihleri (tema, hedef) için genişletilebilir.
 */

export const UserProfile = () => {
    return null;
};
